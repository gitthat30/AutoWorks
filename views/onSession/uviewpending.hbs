{{> wrapper_header}}

<div class="container">
	<h1 class="title">Pending for Confirmation</h1>
	<p class="error-msg">{{error_msg}}</p>
	{{#each req}}
		{{>requestsasuser}}
	{{/each}}
</div>

{{> wrapper_footer}}